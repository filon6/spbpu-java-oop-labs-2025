1. Сколько потоков в вашей программе? (worker + daemon + supervisor + main = 4)


2. Что такое volatile? За счёт чего он работает (запрет кэширования)


3. Что означает:
```public synchronized void foo() {} ```
(Эквивалент synchronized (this){}, который работает с монитором текущего объекта)


4. Как и где запускается поток AbstractProgram?